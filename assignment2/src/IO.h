#ifndef ASS2_V2_IO_H
#define ASS2_V2_IO_H
#include <string>

// Reads and executes all commands in input file, and writes to output file.
void readAndExecuteCommands(std::string inputFileName,std::string outputFileName);

#endif